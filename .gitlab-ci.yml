image: docker:latest
services:
 - docker:dind

stages:
 - deploy

step-backend:
 stage: deploy
 script:
  - sudo apt-get install -y python-pip
  - sudo pip install docker-compose
  - sudo docker-compose -f Project/docker-compose.yml build --no-cache
  - sudo docker-compose -f Project/docker-compose.yml up -d